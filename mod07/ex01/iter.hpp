#ifndef ITER_HPP
#define ITER_HPP

template<typename Add, size_t length, typename Fun>
void    iter(Add *address, size_t length, Fun ft)
{
    for (size_t i = 0; i < length; i++)
    {
        /* code */
    }
    
}

#endif